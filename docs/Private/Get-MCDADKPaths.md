<!-- Generated by build task Generate_Repo_Docs. Do not edit manually. -->
# Get-MCDADKPaths

Source: `source/Private/Workspace/BootImage/New-MCDWinPEBootImage.ps1`

## Synopsis

Resolves ADK paths for a specific architecture.

## Description

Internal helper function that builds a paths object for the Windows ADK based
on the installed ADK information and target architecture. Handles the ARM64
etfsboot.com redirect (falls back to amd64 if ARM64 version is missing).

## Parameters

| Name | Type | Mandatory | Pipeline | Description |
| --- | --- | --- | --- | --- |
| ADKInfo | Object | Yes | No | An ADKInstallerModel object containing ADK installation information. |
| Architecture | String | Yes | No | Target architecture. Valid values are 'amd64' or 'arm64'. |

## Examples

### Example 1

```powershell
$paths = Get-MCDADKPaths -ADKInfo $adkInfo -Architecture 'amd64'

Returns a hashtable with all ADK paths for the amd64 architecture.
```
