<!-- Generated by build task Generate_Repo_Docs. Do not edit manually. -->
# Start-MCDWorkspace

Source: `source/Public/Start-MCDWorkspace.ps1`

## Synopsis

Starts the MCD Workspace environment for creating and managing deployment media.

## Description

This function initializes the MCD Workspace environment on a full Windows system.
It validates prerequisites, loads or creates configuration, initializes the workspace
directory structure, and optionally launches the graphical user interface for
managing WinPE deployment media.

## Parameters

| Name | Type | Mandatory | Pipeline | Description |
| --- | --- | --- | --- | --- |
| Name | String | No | No | The name of the workspace to initialize or use. Defaults to 'Default'. Each workspace has its own directory structure and configuration.   |
| ConfigPath | String | No | No | The path to the MCD configuration file. If not specified, uses the default location at $env:ProgramData\MCD\config.json.   |
| NoGui | SwitchParameter | No | No | If specified, skips launching the graphical user interface and returns the initialized workspace object. Useful for scripted operations and testing.   |

## Examples

### Example 1

```powershell
Start-MCDWorkspace

Starts the workspace with default settings and launches the GUI.
```

### Example 2

```powershell
Start-MCDWorkspace -Name 'ProductionDeploy' -NoGui

Initializes a named workspace without launching the GUI, returning the workspace object.
```

### Example 3

```powershell
Start-MCDWorkspace -ConfigPath 'D:\MCD\custom-config.json' -Verbose

Starts the workspace using a custom configuration file with verbose output.
```


